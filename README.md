
The most important channels in the HSV colorspace are hue and saturation because those two detect color, so you can ignore the value channel. You could also ignore saturation, but keeping hue and saturation provides the most robust solution against changes in light. Before changing the if statement to a for loop, holding up two objects of the same color result in the boundary bouncing from object to object without being able to recognize both. 





